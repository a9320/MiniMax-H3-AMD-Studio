# ============================================================
# MiniMax H3 视频生成创空间 依赖清单
# 注意：torch / torchvision / torchaudio 已注释 ——
# 魔搭创空间镜像已预装适配的 PyTorch，重装会覆盖 CUDA/ROCm 版本（文章第二节关键认知）
# ============================================================

# PyTorch 核心库（已注释，使用镜像预装版本）
# torch
# torchvision
# torchaudio

# ---- ComfyUI 核心依赖（源码随仓库分发，见 scripts/setup_comfyui.sh）----
comfyui-embedded-docs>=0.1.0
comfyui_frontend_package>=0.1.0
comfyui_workflow_templates>=0.1.0
torchsde>=0.2.0,<1.0.0
einops>=0.6.0,<1.0.0
transformers>=4.28.1,<5.0.0
tokenizers>=0.13.3,<1.0.0
sentencepiece>=0.1.99,<1.0.0
safetensors>=0.4.2,<1.0.0
aiohttp>=3.8.0,<4.0.0
yarl>=1.8.0,<2.0.0
pyyaml>=6.0,<7.0.0
Pillow>=9.0.0,<11.0.0
scipy>=1.9.0,<2.0.0
tqdm>=4.64.0,<5.0.0
psutil>=5.9.0,<6.0.0
kornia>=0.7.1,<1.0.0
spandrel>=0.1.0,<1.0.0
soundfile>=0.11.0,<1.0.0
# av 10.x/11.x 在 Py3.12 下无预编译 wheel，pip 会退回源码编译并因缺少 ffmpeg 开发库失败，
# 必须用 12+ 的预编译 wheel（packages.txt 里同时装了 ffmpeg 开发库兜底）
av>=12.0.0,<17.0.0

# ---- 创空间应用本身 ----
# Gradio 由魔搭平台按 SDK 配置统一安装（本仓库不钉版本，避免与镜像/launcher 冲突）
requests>=2.28.0,<3.0.0
modelscope>=1.20.0,<2.0.0

# ---- 可选依赖（用于更好的用户体验）----
# 如果需要更好的日志管理和性能监控
# python-dotenv>=1.0.0  # 用于 .env 文件支持
